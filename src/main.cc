#include <iostream>

#include "board.hpp"

int main() {
  // Board b;
  // InitBoard(b);
  // std::cout << BoardToStr(b) << std::endl;
  // DropDiskToBoard(b, DiskType::kPlayer1, 6);
  // std::cout << BoardToStr(b) << std::endl;
}